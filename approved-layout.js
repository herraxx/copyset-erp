/* CopySet ERP — legacy approved-layout compatibility shim.
   Tarjoukset and Tilaukset list ownership moved to quotes-list.js and orders-list.js.
   Keep this file temporarily so the clean branch can retire the old include safely. */
(function(){
  window.CopySet=window.CopySet||{};
})();